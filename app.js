let language=navigator.language.toLowerCase().startsWith('tr')?'tr':'en';const langButton=document.querySelector('#language');function translate(){document.documentElement.lang=language;document.querySelectorAll('[data-tr]').forEach(el=>el.textContent=el.dataset[language]);langButton.textContent=language==='tr'?'EN':'TR';document.title=language==='tr'?'Lunahan — Yapay Zekâ Asistanı':'Lunahan — AI Assistant'}function show(id){document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===id));location.hash=id;window.scrollTo({top:0,behavior:'smooth'})}document.querySelectorAll('[data-view]').forEach(button=>button.addEventListener('click',()=>show(button.dataset.view)));langButton.addEventListener('click',()=>{language=language==='tr'?'en':'tr';translate()});document.querySelector('#settingsLanguage').addEventListener('click',()=>langButton.click());document.querySelectorAll('.draft').forEach(button=>button.addEventListener('click',()=>show('drafts')));document.querySelectorAll('.actions .primary').forEach(button=>button.addEventListener('click',()=>alert(language==='tr'?'Demo: Gerçek gönderim için Gmail bağlantısı ve son onay gerekir.':'Demo: A Gmail connection and final approval are required to send.')));translate();show(location.hash.slice(1)||'home');
